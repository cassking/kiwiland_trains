describe('components', () => {
  describe('FileInput', () => {
    let target;

    beforeEach(() => {

    });

    describe('#onChange', () => {
      it('fail', () => {
        expect(true).toEqual(false);
      });
    });

    describe('#render', () => {
      it('fail', () => {
        expect(true).toEqual(false);
      });
    });
  });
});
