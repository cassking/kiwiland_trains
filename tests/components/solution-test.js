describe('components', () => {
  describe('Solution', () => {
    describe('#getInitialState', () => {
      it('fail', () => {
        expect(true).toEqual(false);
      });
    });

    describe('#onNetworkChange', () => {
      it('fail', () => {
        expect(true).toEqual(false);
      });
    });

    describe('#onReadFile', () => {
      it('fail', () => {
        expect(true).toEqual(false);
      });
    });

    describe('#render', () => {
      it('fail', () => {
        expect(true).toEqual(false);
      });
    });
  });
});
