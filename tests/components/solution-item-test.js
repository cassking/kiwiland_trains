describe('components', () => {
  describe('SolutionItem', () => {
    describe('#render', () => {
      it('should return null', () => {
        expect(target.render()).toBeNull();
      });
    });
  });
});
