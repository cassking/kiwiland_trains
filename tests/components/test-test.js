describe('components', () => {
  describe('Test', () => {
    describe('#componentDidMount', () => {
      it('fail', () => {
        expect(true).toEqual(false);
      });
    });

    describe('#render', () => {
      it('fail', () => {
        expect(true).toEqual(false);
      });
    });
  });
});
